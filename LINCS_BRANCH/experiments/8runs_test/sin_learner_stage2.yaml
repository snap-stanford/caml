program: metalearn/train_caml_lincs.py
method: grid
metric:
  goal: minimize
  name: "best_val_mse_diff_20_value"

parameters:
  meta_batch_size:
    values: [1]

  batch_size:
    values: [20] ## add 4

  num_como_layers:
    values: [2]

  num_covariates_layer:
    values: [2]

  num_propensity_layers:
    values: [6]

  num_treatment_layer:
    values: [4]

  pro_dropout:
    values: [ 0.1]

  dropout:
    values: [0]

  l1_reg:
    values: [1e-7]

  dim_output:
    values: [128]

  dim_hidden_treatment:
    values: [128]

  dim_hidden_covariates:
    values: [256]

  meta_learning_rate:
    values: [0.1]

  dim_hidden_como:
    values: [128]

  dim_hidden_propensity:
    values: [16]

  model_dim:
    values: [512]

  learning_rate:
    values: [0.0001]

  lr:
    values: [0.003]

  pro_lr:
    values: [0.0003]

  weight_decay:
    values: [0.1]

  sin_weight_decay:
    values: [0.005]

  pro_weight_decay:
    values: [0.005]

  gnn_weight_decay:
    values: [0.005]

  caml_k:
    values: [1]

  sin_learner:
    values: ["true"]

  task_embedding_config:
    values: ["early_concat"]

  use_task_embeddings:
    values: ["true"]

  step:
    values: ["tau"]

  n_iterations:
    values: [50000]

  val_interval:
    values: [50]

  sin_learner_stage1_loc:
    values: ['lincs-critical/model_13900.pt']

  output_suffix:
    values: ['sin_learner_table2']

  dummy_repetition:
    values: [0,1,2,3,4,5,6,7]



